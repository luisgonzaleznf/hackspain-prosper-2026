{
  "mcpServers": {
    "context7": {
      "comment": "Live, current library docs. PLACEHOLDER launch command — confirm for your install (e.g. an API key may be required) and update before relying on it. See README 'MCP servers'.",
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"]
    },
    "claude-in-chrome": {
      "comment": "Drive and screenshot the running app (used by /verify and /demo). PLACEHOLDER launch command — confirm the exact invocation for your installed Claude-in-Chrome version and update before relying on it. See README 'MCP servers'.",
      "command": "npx",
      "args": ["-y", "@anthropic-ai/claude-in-chrome-mcp"]
    }
  }
}
