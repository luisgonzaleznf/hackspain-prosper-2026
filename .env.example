# Copy to .env. Never commit real credentials.
VOICE=codex
PORT=7860
DATABASE_PATH=data/clinic.sqlite3
CALLS_DIR=logs/phone-calls
AUDIO_DIR=logs/phone-audio
PUBLIC_WEBSOCKET_URL=wss://rosario.fyi/integrations/twilio/ws

# Browser WebRTC relay configuration can be supplied to the Pipecat runner.
# PIPECAT_ICE_SERVERS=[...]

# Optional model providers.
OPENAI_API_KEY=
GEMINI_API_KEY=
