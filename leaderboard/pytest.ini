; Keep pytest rooted in this package: `uv run --project . python -m pytest`
; run from here tests THIS copy of app/, not the repo root's.
[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
